extends layouts/base.pug

block content
  .row.justify-content-center.mt-5
    .col-md-5.text-center
      p(style="font-size:4rem;font-family:'Playfair Display',serif;color:var(--amber)") 404
      h3.mb-3(style="font-weight:400") algo salió mal
      p(style="color:var(--muted);font-size:13px")= message || 'ocurrió un error inesperado.'
      a.btn.btn-amber.mt-3(href="/") volver al inicio